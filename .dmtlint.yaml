linters-settings:
  openapi:
    exclude-rules:
      enum:
        - spec.versions[0].schema.openAPIV3Schema.properties.spec.properties.cephVersion
  container:
    exclude-rules:
      controller-security-context:
        - kind: Deployment
          name: sds-elastic
